import express from "express";


const subscriberRouter = express.Router();

subscriberRouter.post("/create-subscriber", );
subscriberRouter.get("/retrieve-subscriber", );

export default subscriberRouter;